export function инвертироватьСтроку(строка: string): string {
    // Возвращает строку, которая является перевернутой версией исходной строки
    // Split разделяет строку на массив
    return строка.split('').reverse().join("");
}
