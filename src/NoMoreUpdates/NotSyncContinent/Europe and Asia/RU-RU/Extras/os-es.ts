// os-ru.ts
import {
    EOL,
    arch as архитектура,
    cpus as процессоры,
    endianness as порядокБайтов,
    freemem as свободнаяПамять,
    homedir as домашнийКаталог,
    hostname as имяХоста,
    loadavg as средняяНагрузка,
    networkInterfaces as интерфейсыСети,
    platform as платформа,
    release as версияОС,
    tmpdir as временныйКаталог,
    totalmem as общаяПамять,
    uptime as времяАктивности,
    userInfo as информацияПользователя,
} from 'os';

// Экспортировать константы и функции
const операционнаяСистема = {
    EOL, // Конец строки
    архитектура, // Возвращает архитектуру процессора
    процессоры, // Информация о процессорах
    порядокБайтов, // Возвращает порядок байтов
    свободнаяПамять, // Возвращает свободную память в байтах
    домашнийКаталог, // Возвращает домашний каталог текущего пользователя
    имяХоста, // Возвращает имя хоста
    средняяНагрузка, // Возвращает среднюю нагрузку системы
    интерфейсыСети, // Информация об интерфейсах сети
    платформа, // Возвращает платформу операционной системы
    версияОС, // Возвращает версию операционной системы
    временныйКаталог, // Возвращает временный каталог
    общаяПамять, // Возвращает общую память в байтах
    времяАктивности, // Возвращает время активности системы
    информацияПользователя, // Информация о текущем пользователе
};

// Экспортировать модуль на русском
export default операционнаяСистема;

// Вспомогательные функции с переведенными названиями
export function получитьИнформациюОС() {
    return {
        архитектура: операционнаяСистема.архитектура(),
        имяХоста: операционнаяСистема.имяХоста(),
        домашнийКаталог: операционнаяСистема.домашнийКаталог(),
        свободнаяПамять: операционнаяСистема.свободнаяПамять(),
        общаяПамять: операционнаяСистема.общаяПамять(),
        времяАктивности: операционнаяСистема.времяАктивности(),
        средняяНагрузка: операционнаяСистема.средняяНагрузка(),
        интерфейсыСети: операционнаяСистема.интерфейсыСети(),
        версияОС: операционнаяСистема.версияОС(),
    };
}
