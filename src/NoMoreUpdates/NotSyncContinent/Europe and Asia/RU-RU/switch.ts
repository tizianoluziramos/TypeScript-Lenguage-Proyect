export function случай(значение: any, ...опции: { условие: any, блок: () => void }[]) {
    for (const { условие, блок } of опции) {
        if (значение === условие) {
            блок();
            return;
        }
    }
}
